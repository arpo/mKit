# Ignore node_modules, as dependencies will be installed inside the container
node_modules

# Ignore source TypeScript files, as we only need the compiled JS in dist/
src/
*.ts
tsconfig.json
tsconfig.server.json

# Ignore development/build tooling configuration
eslint.config.js
nodemon.json
browser-sync.config.cjs
.gitignore
.git
.vscode

# Ignore build output directory if it exists locally (it will be copied from build stage if multi-stage build is used, but here we copy it directly)
# dist # Keep this commented out as we copy dist/ in the Dockerfile

# Ignore documentation and lock files if not needed in the final image
README.md
package-lock.json # Keep this commented out - needed by npm ci

# Ignore the Dockerfile itself
Dockerfile
.dockerignore
